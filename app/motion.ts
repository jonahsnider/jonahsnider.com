'use client';

import { motion } from 'framer-motion';

// This file is a workaround until https://github.com/framer/motion/issues/1924 is fixed

export const div = motion.div;
export const p = motion.p;
